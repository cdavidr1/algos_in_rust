mod dsu;
mod fenwick_tree;
mod graph;
mod priority_queue;
mod queue;
mod segment_tree;
mod stack;

fn main() {
    let concat = concat_str("abc", "def");
}

fn concat_str(a: &str, b: &str) -> String {
    a.to_string() + b
}
